package com.example.pages.generated;

// ============================================================
// ⚠️  AUTO-GENERATED FILE — DO NOT EDIT MANUALLY
// Generated by JavaLive Annotation Processor
// Source: com.example.pages.UserManagement
// Component: user-management
// ============================================================

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.messaging.handler.annotation.*;
import org.springframework.messaging.simp.SimpMessagingTemplate;
import org.springframework.stereotype.Controller;
import io.javalive.core.session.LiveSessionManager;
import io.javalive.core.state.StateManager;
import io.javalive.core.state.StateDiff;
import io.javalive.core.dispatch.MethodDispatcher;
import io.javalive.core.dispatch.DispatchResult;
import io.javalive.core.dispatch.SecurityGuard;
import io.javalive.core.websocket.ClientMessage;
import io.javalive.core.websocket.ServerMessage;
import com.example.pages.UserManagement;
import java.util.Map;

@Controller
@SuppressWarnings({"unchecked", "unused"})
public class UserManagementLiveController {

    @Autowired
    private SimpMessagingTemplate messagingTemplate;

    @Autowired
    private LiveSessionManager sessionManager;

    @Autowired
    private StateManager stateManager;

    @Autowired
    private MethodDispatcher methodDispatcher;

    // ─────────────────────────────────────────────────────────
    // Init Handler — sends the initial state to the browser
    // ─────────────────────────────────────────────────────────

    @MessageMapping("/user-management.init")
    public void handleInit(
            @Header("simpSessionId") String sessionId) {

        // Create or retrieve the session state for this component
        Map<String, Object> initialState = stateManager.getOrCreate(
            sessionId, "user-management");

        // Execute @VueLifecycle(hook="onMounted") methods
        DispatchResult mountResult = methodDispatcher.dispatchLifecycle(
            "com.example.pages.UserManagement",
            "onMounted",
            initialState);
        if (mountResult.getNewState() != null) {
            initialState = mountResult.getNewState();
            stateManager.setState(sessionId,
                "user-management", initialState);
        }

        // Send full state to this specific session
        messagingTemplate.convertAndSendToUser(
            sessionId,
            "/topic/user-management.state",
            ServerMessage.fullState(initialState)
        );
    }

    // ─────────────────────────────────────────────────────────
    // Method Handlers — generated from @VueMethod annotations
    // ─────────────────────────────────────────────────────────

    @MessageMapping("/user-management.search")
    public void handle_search(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "search");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "search",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    @MessageMapping("/user-management.goToPage")
    public void handle_goToPage(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "goToPage");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "goToPage",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    @MessageMapping("/user-management.openCreateModal")
    public void handle_openCreateModal(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "openCreateModal");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "openCreateModal",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    @MessageMapping("/user-management.closeCreateModal")
    public void handle_closeCreateModal(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "closeCreateModal");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "closeCreateModal",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    @MessageMapping("/user-management.createUser")
    public void handle_createUser(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "createUser");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "createUser",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    @MessageMapping("/user-management.deleteUser")
    public void handle_deleteUser(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "deleteUser");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "deleteUser",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    @MessageMapping("/user-management.clearSearch")
    public void handle_clearSearch(
            ClientMessage message,
            @Header("simpSessionId") String sessionId) {

        // Security check: ensure only @VueMethod annotated methods can be called
        SecurityGuard.verify(
            "com.example.pages.UserManagement",
            "clearSearch");

        // Retrieve current session state
        Map<String, Object> currentState = stateManager.getState(
            sessionId, "user-management");

        // Dispatch to the actual Java method
        DispatchResult result = methodDispatcher.dispatch(
            "com.example.pages.UserManagement",
            "clearSearch",
            currentState,
            message.getArgs());

        if (result.hasError()) {
            // Send error back to the originating session
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                ServerMessage.error(result.getError())
            );
            return;
        }

        // Persist new state
        stateManager.setState(sessionId,
            "user-management", result.getNewState());

        // Compute diff and push only changed fields
        StateDiff diff = StateDiff.compute(currentState, result.getNewState());
        if (!diff.isEmpty()) {
            messagingTemplate.convertAndSendToUser(
                sessionId,
                "/topic/user-management.state",
                diff.toServerMessage()
            );
        }
    }

    // ─────────────────────────────────────────────────────────
    // Lifecycle Handlers — generated from @VueLifecycle
    // ─────────────────────────────────────────────────────────

}
