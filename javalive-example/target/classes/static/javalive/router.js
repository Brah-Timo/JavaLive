// ============================================================
// ⚠️  AUTO-GENERATED FILE — DO NOT EDIT MANUALLY
// Generated by JavaLive Annotation Processor
// Contains routes for all @VuePage components
// ============================================================

import { createRouter, createWebHistory } from 'vue-router';

// ── Route definitions ─────────────────────────────────────────
const routes = [
  {
    path: '/dashboard',
    name: 'Dashboard',
    component: () => import('./components/dashboard.js'),
    meta: {
      requiresAuth: false,
      layout: 'default',
      title: 'Dashboard',
    },
  },
  {
    path: '/users',
    name: 'UserManagement',
    component: () => import('./components/user-management.js'),
    meta: {
      requiresAuth: false,
      layout: 'default',
      title: 'UserManagement',
    },
  },
];

// ── Navigation Guards ─────────────────────────────────────────
export const router = createRouter({
  history: createWebHistory(),
  routes,
  scrollBehavior(to, from, savedPosition) {
    if (savedPosition) return savedPosition;
    return { top: 0 };
  }
});

// Auth guard — redirect to /login if route requires auth
router.beforeEach(async (to, from) => {
  if (to.meta.requiresAuth) {
    const isAuthenticated = window.__JAVALIVE_AUTH__ === true;
    if (!isAuthenticated) {
      return { path: '/login', query: { redirect: to.fullPath } };
    }
  }
  // Update page title
  if (to.meta.title) {
    document.title = to.meta.title;
  }
});

export default router;
